@pre |a_0| >= 0
@post true
# (|a_0| + 1)
int[] abs(int[] a_0) {
	int[] a := a_0;
	for
		@ 0 <= i && i <= |a|
		# (|a|)
		(int i := 0; i < |a|; i := i + 1) 
	{
		if (a[i] < 0) {
			a[i] := -a[i];
		}
	}
	return a;
}
